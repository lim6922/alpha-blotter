# Harness.md

## 목적

이 문서는 `alpha-blotter` 프로젝트의 장기 유지보수를 위한 운영 기준서다.
총괄 기획자, 개발자, 디자이너, 후임 담당자는 이 문서를 기준으로 같은 판단 체계를 유지한다.

이 문서의 목표는 다음과 같다.

- 프로젝트의 제품 목적과 도메인 맥락을 빠르게 이해하게 한다.
- 역할별 책임과 의사결정 권한을 명확히 한다.
- 구현 규칙, UI 규칙, 데이터 안전 규칙을 일관되게 유지한다.
- 신규 인수인계자도 `Harness.md`만 읽고 즉시 작업을 시작할 수 있게 한다.

새로운 규칙, 운영 원칙, 아키텍처 결정이 생기면 반드시 이 문서를 먼저 또는 동시에 업데이트한다.

---

## 문서 우선순위

규칙 충돌 시 우선순위는 아래와 같다.

1. 실제 사용자 데이터 보호 원칙
2. `Harness.md`
3. 코드에 반영된 현재 동작
4. README 등 보조 문서

즉, README가 최신이 아니어도 `Harness.md`가 최신 기준 문서다.

---

## 프로젝트 한 줄 정의

`alpha-blotter`는 파생상품 개인 매매를 기록하고, 포지션/손익/리스크/성과를 브라우저에서 분석하는 단일 페이지 블로터 애플리케이션이다.

---

## 제품 범위

현재 프로젝트가 반드시 책임지는 범위는 아래와 같다.

- 체결 입력 및 수정
- 포지션 집계
- 체결 단위 및 포지션 단위 성과 계산
- 활성 포지션 모니터링
- 성과 리포트 및 통합 분석 차트
- CSV Import / Export
- Supabase 기반 로그인/가져오기/내보내기/자동 동기화
- 다중 상품, 다중 통화(KRW/USD) 표시

현재 프로젝트가 직접 책임지지 않는 범위는 아래와 같다.

- 브로커 주문 실행
- 실계좌 주문 연동
- 실시간 체결 체결원장 백오피스
- 세무 신고 자동화
- 투자 자문, 신호 생성, 매매 추천

---

## 도메인 이해

이 프로젝트는 일반 가계부가 아니라 파생상품 거래 기록 도구다. 따라서 아래 개념을 항상 구분해서 다뤄야 한다.

- 체결(Trade): 한 번의 매수/매도 입력 단위
- 포지션(Position): 여러 체결이 합쳐진 실제 보유 상태
- 청산(Close): 기존 포지션을 줄이거나 종료하는 체결
- 평단가(Average Price): 잔여 수량 기준 평균 단가
- 실현손익(Realized PnL): 청산으로 확정된 손익
- 미실현손익(Unrealized PnL): 현재가 기준 평가 손익
- 손익률(PnL%): 기준 원금 또는 진입 기준 대비 수익률
- 스탑로스(Stop Loss): 손실 제한 기준 가격
- DTE: 만기까지 남은 일수
- 통화 기준: 원화(KRW)와 달러(USD)를 동시에 관리함

도메인 규칙은 시각화보다 우선한다. 차트나 테이블이 보기 좋아도 계산 의미가 틀리면 실패다.

---

## 현재 구조 요약

레포 기준 주요 구조는 아래와 같다.

- [index.html](D:/241110_매크로/git/alpha-blotter/index.html): 단일 페이지 UI 골격
- [css/style.css](D:/241110_매크로/git/alpha-blotter/css/style.css): 전체 스타일
- [js/app.js](D:/241110_매크로/git/alpha-blotter/js/app.js): 핵심 상태, 계산, 렌더링, 동기화 로직
- [js/supabase.js](D:/241110_매크로/git/alpha-blotter/js/supabase.js): Supabase 연결 설정
- [README.md](D:/241110_매크로/git/alpha-blotter/README.md): 외부 공개용 개요

현재는 프레임워크 없는 브라우저 기반 구조이며, 많은 핵심 로직이 `js/app.js`에 집중되어 있다.

따라서 변경 작업은 아래 원칙을 따른다.

- 기능 추가 시 계산 로직과 렌더링 로직을 가능한 한 분리한다.
- 도메인 계산 함수는 UI 문자열 생성과 섞지 않는다.
- 장기적으로 파일 분리 여지가 있더라도, 당장 도입하는 변경은 현재 구조 안에서 일관성을 유지해야 한다.

---

## 역할 정의

### 1. 총괄 기획자

총괄 기획자는 파생상품 거래 맥락을 이해하는 제품 책임자다.

책임은 아래와 같다.

- 실제 매매 흐름에 맞는 업무 요구사항 정의
- 손익, 포지션, 리스크 지표의 의미 승인
- UI에서 무엇을 강조하고 무엇을 숨길지 우선순위 결정
- 신규 기능이 실전 기록 업무를 단순화하는지 판단
- 계산 기준 변경 시 최종 승인

총괄 기획자는 반드시 아래를 명확히 전달해야 한다.

- 무엇이 체결 기준인지 포지션 기준인지
- 통화 기준이 KRW인지 USD인지 합산 기준인지
- 손익, 수수료, 스탑, DTE가 표시인지 계산인지
- 리포트에서 비교해야 하는 기준 기간과 자산 단위

### 2. 개발자

개발자는 기능 구현과 안정성의 책임자다.

책임은 아래와 같다.

- 요구사항을 코드와 데이터 흐름으로 해석
- 기존 계산 로직과 충돌 여부 검토
- 데이터 손실 위험이 있는 변경 방지
- 차트/테이블/동기화가 서로 다른 값을 보여주지 않도록 정합성 유지
- `Harness.md` 갱신

개발자는 구현 전에 반드시 아래를 확인한다.

- 이 변경이 체결 단위인지 포지션 단위인지
- 통화 변환 시 `globalFX` 또는 개별 `fxRate` 중 무엇을 쓰는지
- CSV 구조와 Supabase 구조가 같이 바뀌는지
- 기존 `renderAll()` 흐름에 어떤 영향이 있는지

### 3. 디자이너

디자이너는 보기 좋은 화면이 아니라 해석 가능한 화면을 만드는 역할이다.

책임은 아래와 같다.

- 숫자 우선순위와 시선 흐름 설계
- 손익, 위험, 상태를 색/형태/레이블로 안정적으로 구분
- 모바일과 데스크톱 모두에서 읽히는 레이아웃 유지
- 차트의 가독성 개선
- 겹침, 잘림, 과밀 표시 완화

디자이너는 아래 규칙을 지켜야 한다.

- 손익은 색상만으로 구분하지 말고 부호, 배지, 텍스트를 함께 사용한다.
- 중요한 수치는 장식보다 우선한다.
- 금융 UI는 미려함보다 해석 속도와 오류 방지가 우선이다.
- 다크/라이트나 테마 변경보다 숫자 가독성과 정보 밀도 균형이 더 중요하다.

### 4. 데이터/운영 담당자

별도 역할이 없다면 총괄 기획자 또는 개발자가 겸임한다.

책임은 아래와 같다.

- CSV 백업 관리
- Supabase 동기화 정책 점검
- 허용 이메일 및 권한 검토
- 실데이터를 public 레포에 올리지 않도록 관리

### 5. 후임 인수인계자

후임 담당자는 아래 순서로 프로젝트를 이해한다.

1. `Harness.md` 전체 읽기
2. `README.md` 확인
3. [index.html](D:/241110_매크로/git/alpha-blotter/index.html)에서 화면 구조 파악
4. [js/app.js](D:/241110_매크로/git/alpha-blotter/js/app.js)에서 데이터 흐름 확인
5. 로컬에서 주요 시나리오 수동 검증

후임 담당자는 기능을 수정하기 전에 `Harness.md`의 규칙과 실제 코드가 어긋나는지 먼저 점검한다.

---

## 공통 작업 원칙

모든 역할은 아래 원칙을 공유한다.

- 실제 거래 해석과 맞지 않는 기능은 추가하지 않는다.
- 계산 결과는 화면마다 다르게 보이면 안 된다.
- 차트는 장식이 아니라 검증 도구여야 한다.
- 리포트는 체결 기준과 포지션 기준을 혼동하지 않아야 한다.
- 동기화 기능은 편의 기능이지만 데이터 안전보다 우선하지 않는다.
- Public 레포에는 실거래 원본 데이터를 절대 포함하지 않는다.

---

## 구현 규칙

### 데이터 모델 규칙

- 거래 데이터는 가능한 한 원본 의미를 유지한다.
- 계산 결과 필드는 렌더링 직전 또는 계산 함수에서 파생한다.
- 체결 입력값과 계산 결과값을 같은 필드에 덮어쓰지 않는다.
- KRW와 USD를 같은 숫자처럼 섞어 저장하지 않는다.
- 합산 시에는 항상 기준 통화를 명시한다.

### 계산 규칙

- 체결 단위 손익과 포지션 단위 손익은 별개로 유지한다.
- 청산 여부 판정은 시각 효과가 아니라 재고/잔량 로직을 기준으로 한다.
- 손익 계산 기준이 바뀌면 관련된 테이블, 요약, 차트, CSV, 클라우드 스키마를 함께 검토한다.
- 가격 축, 손익 축, 라벨 텍스트는 모두 같은 계산 원천을 사용해야 한다.

### UI 구현 규칙

- 숫자 표시는 `toLocaleString` 기반으로 일관성 있게 처리한다.
- KRW와 USD는 접두 기호와 자릿수 정책을 분명히 구분한다.
- 차트 축 제목과 툴팁은 단위를 생략하지 않는다.
- 포인트 태그가 겹치면 표시량을 줄이거나 위치를 분산한다.
- 여러 상품을 한 차트에 표시할 때는 상품별 시리즈를 기본값으로 한다.

### 차트 규칙

- Y축 가격 범위는 조회 기간의 실제 체결 가격 범위를 기준으로 잡고, 최소한의 버퍼를 둔다.
- 가격 Y축은 필요 시 KRW와 USD를 함께 보여준다.
- 여러 상품이 섞일 경우 상품별 라인을 분리한다.
- 차트 위 태그는 가독성이 떨어지면 항상 우선순위를 재조정한다.
- 누적 손익과 체결 가격은 서로 다른 축을 사용하되, 같은 데이터 포인트를 참조해야 한다.

### 동기화 규칙

- Import는 덮어쓰기 성격이 강하므로 경고 문구와 상태 표시가 필요하다.
- Export는 로컬 최신 입력을 반영하는 행위임을 명확히 한다.
- 자동 동기화는 편의 기능이며, 실패 시 조용히 데이터가 사라지면 안 된다.
- 동기화 상태 UI와 실제 동작은 반드시 일치해야 한다.

---

## 코딩 규칙

이 프로젝트는 현재 프레임워크 없는 구조이므로, 과한 추상화보다 예측 가능한 명시성을 우선한다.

### JavaScript 규칙

- 함수 이름은 역할이 바로 드러나게 작성한다.
- 계산 함수와 DOM 갱신 함수를 가능한 한 분리한다.
- 매직 넘버는 의미가 있으면 상수로 뽑는다.
- 통화 포맷, 손익 포맷, 차트 라벨 포맷은 중복 구현하지 않는다.
- 하나의 변경이 여러 UI 조각에 영향을 주면 공통 헬퍼를 검토한다.
- 브라우저 전역 상태를 추가할 때는 왜 필요한지 코드상에서 읽히게 만든다.

### HTML 규칙

- 섹션은 현재처럼 주석 블록과 카드 단위 구조를 유지한다.
- 텍스트는 한국어 중심으로 유지하되, 도메인 용어는 영문 병기를 허용한다.
- 버튼 텍스트는 실제 행동을 명확히 드러내야 한다.

### CSS 규칙

- 상태 색상은 의미를 유지한다.
- `up`, `down`, `muted`, `accent`, `warn`, `bad` 계열 의미를 함부로 바꾸지 않는다.
- 숫자 가독성을 해치는 과한 장식은 피한다.
- 모바일에서 표와 차트가 깨지면 미완성으로 본다.

---

## 변경 절차

모든 의미 있는 변경은 아래 순서를 기본으로 한다.

1. 요구사항을 체결/포지션/통화/동기화 관점으로 해석한다.
2. 영향을 받는 화면과 계산 경로를 확인한다.
3. 구현 전에 `Harness.md`를 갱신해야 하는지 판단한다.
4. 코드 수정 후 수동 검증을 수행한다.
5. 규칙 또는 구조가 바뀌었다면 `Harness.md`를 업데이트한다.

다음 경우에는 `Harness.md` 업데이트가 필수다.

- 역할이나 승인 체계가 바뀐 경우
- 핵심 계산 기준이 바뀐 경우
- CSV 또는 Supabase 구조가 바뀐 경우
- 차트/리포트 해석 기준이 바뀐 경우
- 인수인계에 필요한 핵심 맥락이 새로 생긴 경우

---

## 수동 검증 체크리스트

변경 후 최소한 아래를 확인한다.

- 체결 입력 후 Trade History에 값이 올바르게 표시되는가
- 포지션 잔량과 평균단가가 예상대로 계산되는가
- 실현손익/미실현손익/누적손익이 서로 모순되지 않는가
- Performance Report 차트 축과 툴팁의 통화/단위가 맞는가
- 여러 상품 선택 시 상품별 라인이 분리되어 보이는가
- CSV Export 후 Import 시 의미 손실이 없는가
- 동기화 로그인/가져오기/내보내기 상태가 UI와 일치하는가
- 모바일 폭에서도 핵심 숫자와 버튼이 읽히는가

---

## 제언 처리 규칙

총괄 기획자 또는 사용자의 제언은 아래 네 가지로 분류한다.

- 계산 정확도 개선
- 가독성 개선
- 업무 흐름 단축
- 데이터 안전성 강화

우선순위는 아래 순서를 기본으로 한다.

1. 계산 정확도
2. 데이터 안전성
3. 가독성
4. 편의 기능

즉, 보기 좋은 개선보다 계산과 동기화 안전이 우선이다.

---

## 인수인계용 빠른 이해 포인트

후임자가 가장 먼저 이해해야 할 사실은 아래와 같다.

- 이 앱은 체결 입력 도구이자 성과 분석 도구다.
- 핵심 계산은 `js/app.js`에 집중되어 있다.
- `renderAll()`이 여러 화면의 재계산과 갱신 흐름의 중심이다.
- 통화는 KRW/USD 혼합 구조다.
- CSV와 Supabase 모두 실제 운영 경로다.
- Public 레포에는 실데이터를 두지 않는다.
- 차트는 보여주기용이 아니라 검증용이다.

---

## 유지보수 금지 사항

- 실거래 원본 데이터를 public 레포에 커밋하지 않는다.
- 계산 의미를 바꾸면서 문서를 갱신하지 않는다.
- 차트 값과 표 값이 다르게 보이는데 시각 보정으로 숨기지 않는다.
- 체결 기준과 포지션 기준을 혼합한 지표를 이름 없이 노출하지 않는다.
- 통화가 다른 수치를 설명 없이 같은 열이나 축에 합쳐놓지 않는다.

---

## 향후 리팩터링 방향

장기적으로 아래 방향을 권장한다.

- `js/app.js`의 계산/렌더링/동기화 모듈 분리
- 통화 포맷 및 손익 포맷 헬퍼 분리
- 차트 데이터 생성 로직 분리
- CSV 스키마와 Supabase 매핑 구조 문서화
- 핵심 계산 함수에 대한 테스트 도입

단, 리팩터링은 기능 정합성을 해치지 않는 범위에서 단계적으로 수행한다.

---

## 문서 갱신 규칙

`Harness.md`는 살아 있는 운영 문서다.

규칙은 아래와 같다.

- 새로운 규칙이 생기면 작업과 함께 문서를 갱신한다.
- 규칙이 폐기되면 삭제하지 말고 대체 규칙으로 명시적으로 바꾼다.
- 큰 판단 변경이 있으면 변경 이유를 짧게 남긴다.
- 후임자가 읽었을 때 “왜 이렇게 만들었는지”가 드러나야 한다.

권장 운영 방식은 아래와 같다.

- 기능 PR 또는 커밋에 `Harness.md` 반영 여부를 함께 점검
- 큰 기능 추가 전후로 문서 차이를 검토
- 월 단위 또는 버전 단위로 문서 정합성 점검

---

## README.md 업데이트 규칙

- `README.md`는 외부 공개 기준의 개요 문서로 유지한다.
- `README.md`에는 실제 개인 거래 데이터, 비공개 운영 정보, 내부 자격 정보, 보안 설정값을 적지 않는다.
- 사용자가 직접 체감하는 기능 변화가 있으면 같은 작업 단위에서 `README.md` 업데이트 필요 여부를 함께 판단한다.
- 아래 항목이 바뀌면 `README.md`를 반드시 검토한다.
  - 실행 방법
  - 주요 기능 목록
  - 데이터 입출력 방식
  - 동기화 방식
  - 저장소 구조
- 세부 도메인 규칙, 계산 규칙, 인수인계 기준은 `Harness.md`에 두고 `README.md`에는 요약만 둔다.
- `README.md`는 현재 배포 기준과 일치해야 하며, 더 이상 유효하지 않은 설명은 남겨두지 않는다.
- 문서만 읽는 사람도 앱의 목적, 실행 위치, 데이터 분리 원칙을 바로 이해할 수 있어야 한다.

## history.md 규칙

- `history.md`는 개발 히스토리를 누적적으로 관리하는 운영 문서다.
- `history.md`는 append-only 원칙을 기본으로 하며, 기존 기록을 삭제하거나 덮어쓰지 않는다.
- 기존 기록의 정정이 필요하면 과거 항목을 바꾸는 대신 정정 항목을 후속으로 추가한다.
- 의미 있는 변경이 있으면 코드 변경과 같은 작업 단위에서 `history.md`를 함께 갱신한다.
- 아래 변경은 `history.md` 기록 대상이다.
  - 기능 추가, 수정, 제거
  - 계산 기준 변경
  - 차트/리포트 해석 기준 변경
  - CSV/Supabase 스키마 변경
  - 동기화 동작 변경
  - 문서 운영 규칙 변경
- 각 항목은 최소한 아래 정보를 포함한다.
  - 날짜
  - 변경 요약
  - 상세 내용
  - 영향 범위
  - 검증 여부 또는 미검증 사유
- `history.md`는 홍보 문서가 아니라 변경 맥락을 복원하기 위한 운영 로그여야 한다.
- 추측이나 계획만 적지 않고, 실제 반영된 내용만 기록한다.

---

## 변경 이력

### 2026-04-05

- `Harness.md` 최초 작성
- 역할 정의, 도메인 규칙, 차트 규칙, 코딩 규칙, 인수인계 기준 수립
- `README.md` 업데이트 규칙 추가
- `history.md` 운영 규칙 추가

---

## 인코딩 점검 및 패치 규칙

인코딩 깨짐은 계산 오류 못지않게 운영 리스크를 만든다. 문서/코드 변경 시 아래 규칙을 기본으로 적용한다.

### 인코딩 점검 규칙

- 파일 수정 전 현재 파일 인코딩(UTF-8/UTF-8 BOM/CP949 등)과 줄바꿈(LF/CRLF)을 먼저 확인한다.
- 기존 인코딩을 유지하는 것을 기본 원칙으로 하며, 인코딩 변경은 명시적 목적이 있을 때만 수행한다.
- 한글 텍스트가 포함된 파일은 저장 직후 깨짐(문자 치환, 물음표 대체, mojibake) 여부를 즉시 재확인한다.
- 인코딩 변경이 불가피하면 변경 범위, 이유, 영향 파일을 `history.md`에 기록한다.

### 패치/저장 규칙

- 대용량 문자열 치환 또는 파일 전체 재저장은 인코딩 손상 위험이 있으므로 우선 `apply_patch` 기반의 최소 라인 수정을 사용한다.
- 파일 전체를 다시 쓰는 방식(`Set-Content`, 에디터 전체 저장 등)은 인코딩/줄바꿈이 보장될 때만 사용한다.
- 패치 적용 후 `git diff`에서 의도하지 않은 전역 문자 변경(주석/한글/기호)이 보이면 즉시 중단하고 원복 후 재적용한다.
- UI 텍스트(특히 한국어 라벨/버튼) 변경 시 브라우저 표시와 소스 diff를 함께 확인해 인코딩 이상을 검증한다.

### 검증 체크

- 변경 파일의 한글 라벨이 정상 표기되는가
- `git diff`가 의도한 라인만 변경됐는가
- 동일 파일에서 줄바꿈/인코딩만 대량 변경되지 않았는가
